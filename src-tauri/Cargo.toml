[package]
name = "time-machine-inspector"
version = "1.1.3"
description = "A Tauri App"
default-run = "time-machine-inspector"
edition = "2018"
build = "src/build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "1.0.0-rc.8", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "1.0.0-rc.10", features = ["dialog-message", "macos-private-api", "shell-open"] }
window-vibrancy = { git = "https://github.com/tauri-apps/window-vibrancy" }
plist = { version = "1.3.1", features = ["serde"] }
open = "2.1"

[features]
default = [ "custom-protocol" ]
custom-protocol = [ "tauri/custom-protocol" ]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
